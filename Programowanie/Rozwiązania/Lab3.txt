Andrzej Manderla Laboratorium 2
Zad 1.
#include <stdio.h>
#include <conio.h>
#include <locale.h>
#pragma warning(disable : 4996)
#define _CRT_SECURE_NO_WARNINGS
#define _USE_MATH_DEFINES
#include <math.h>

int main()
{
	setlocale(LC_ALL, "");
	char ch;
	printf("Wypisywanie znaków mo¿na zakoñczyæ CTRL + Z\n");
	while (ch != 26)
	{
		ch = getch();
		printf("%c", ch);
	}
}

Zad 2.
#include <stdio.h>
#include <conio.h>
#include <locale.h>
#pragma warning(disable : 4996)
#define _CRT_SECURE_NO_WARNINGS
#define _USE_MATH_DEFINES
#include <math.h>

int main()
{
	setlocale(LC_ALL, "");
	const int n = 100;
	char text[n];
	int i = 0;
	printf("WprowadŸ tekst do sprawdzenia(maks %i znaków)\n", n);
	fgets(text, n, stdin);

	for (int j = 0; j < n; j++)
		if (text[j] == 'a' || text[j] == 'A')
			i++;

	printf("W tekœcie znajodwa³o siê %i znaków 'a'", i);

	_getch();
}

Zad 3.